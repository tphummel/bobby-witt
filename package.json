{
  "scripts": {
    "test": "standard && tap",
    "fix": "standard --fix",
    "only": "standard && tap --only"
  },
  "devDependencies": {
    "standard": "^16.0.3",
    "tap": "^15.0.9"
  },
  "tap": {
    "check-coverage": false
  }
}
